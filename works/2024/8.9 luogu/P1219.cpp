#include <bits/stdc++.h>
#define int long long
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

int sum=0

int find(int start)
{
    if(start>n)
    {
        sum++;
    }
}

signed main() 
{
    int n;
    cin>>n;
    find(1);
    return 0;
}