#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

bool cmp(ll a,ll b);
{
    return a>b;
}

int main() 
{
    ll n,t,p;
    cin>>n>>t>>p;
    ll l[n];
    ll 
    for(ll i=0;i<n;i++)
    {
        cin>>l[i];
    }
    sort(l,l+n,cmp);

    return 0;
}