#include<bits/stdc++.h>
using namespace std;

int string_to_int(string a)//在读取文件时便于转化为数字

void product_find(int pnum)//读取仓库文件：输出仓库库存

string custom_paasword_find(string name)//返回密码

bool custom_name_find(string name)//判断用户名称是否存在

void sum_up()//购物车结账

void product_up_to_date(int pnum)//更新最终仓库文件：1、购物者导致库存量改变；1、管理者添加货品

void custom_history_up_to_date()//程序结束更新文件中的信息到数组中；改变每个用户的购买记录

void process_shop()//购物结束，处理信息：1、sum_up()  2、product_up_to_date(int pnum)   3、custom_history_up_to_date()

void shop_car()//消费者开始购物

void menu_custom()//消费者界面

void product_add(int pnum)//管理者：添加商品

void admin_space()//管理者界面

void login()//消费者登录

void initi_custom_file()//消费者注册用户文件

void custom_space()//消费者选择：注册或登录

void up_to_date_product(int pnum)//把所有文件的数据读入数组，方便后续排序，更改文件等；同时更新上一次操作导致的仓库余量不同




int main()
