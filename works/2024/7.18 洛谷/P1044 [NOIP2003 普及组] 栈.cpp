#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ull;
typedef long long ll;

ll f[20][20];

void dfs(int x,int y)
{
    if(f[])
}

int main() 
{
    memset(f,-1,sizeof(f));
    int n;
    cin>>n;
    dfs(n,0);
    return 0;
}